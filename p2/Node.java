public final class Node<T extends Comparable<T>> implements Comparable<Node<T>>
{
    /**
        1. declare the five private instance variables as described in the specs
        2. declare the two constructors as described in the specs
        3. declare the five getter methods as described in the specs
        4. declare the five setter methods as described in the specs
        5. declare the compareTo required by the implementation of the Comparable<Node<T>> interface
    */    
}
